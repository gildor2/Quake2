- disabling lightmaps on some surfaces (trans, alpha - neon messages etc.) -- do it with spec. shader keyword,
  don't specify LM stage in shader or another way
? enable auto water bulge (may do with local shaders too)
- map scale (mostly for KP)
? sun/ambient light
- change sky
- coronas (size, color, lightstyle, position, radius (for placing inside objects, e.g. lamp))
- inline shaders
- surface sounds (local materials.lst)
- C-style preprocessor (#include, at least) -- may be, inside COM_Parse?
- entity editor (not convert, just "add"/"remove")
- shader manipulations (FAKK2)

------------------------ Shader extensions ---------------------------

- shader texplates: specify name mask instead of exact shader name before {script}
- map $texture     -- map image with the same name as shader name (useful for shader templates)
- mapsize <w> <h>  -- change image sizes (for Q2 maps: allows to replace texture without resampling)
